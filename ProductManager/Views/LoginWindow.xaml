<Window x:Class="ProductManager.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Height="250"
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Username -->
        <TextBlock Grid.Row="0" Text="Username"/>
        <TextBox Grid.Row="1"
                 x:Name="txtUsername"
                 Margin="0,5,0,15"/>

        <!-- Password -->
        <TextBlock Grid.Row="2" Text="Password"/>
        <PasswordBox Grid.Row="3"
                     x:Name="txtPassword"
                     Margin="0,5,0,20"/>

        <!-- Login Button -->
        <Button Grid.Row="4"
                Content="Login"
                Height="35"
                Width="100"
                HorizontalAlignment="Center"
                Click="Login_Click"
                IsDefault="True"/>
    </Grid>
</Window>
