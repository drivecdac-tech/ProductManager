<Window x:Class="ProductManager.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Height="250"
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Username"/>
        <TextBox x:Name="txtUsername" Margin="0,5,0,15"/>

        <TextBlock Text="Password" Grid.Row="1"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="2" Margin="0,5,0,15"/>

        <Button Grid.Row="3"
                Content="Login"
                Height="35"
                Click="Login_Click"/>
    </Grid>
</Window>
